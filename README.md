## Installation
run the command
```bash
composer install
```
create .env file and database 
and run the commands

```bash
php artisan key:generate
php artisan migrate -seed
npm install
npm run dev
php artisan serve
```
Open the site
